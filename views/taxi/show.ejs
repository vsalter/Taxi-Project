<%- include('../partials/userHeader') %>

        <div class="currentTaxi">
          <table class="table">
              <thead>
                  <tr>
                  <th>User Name:</th>
                  <th>User Email:</th>
                  <th>Type of Car</th>
                  <th>Pickup Address</th>
                  <th>Pickup City</th>
                  <th>Time of Trip</th>
                  </tr>
              </thead>
              <tbody>
                  <tr>
                      <td><%= taxi.userName %></td>
                      <td><%= taxi.userEmail %></td>
                      <td><%= taxi.carType %></td>
                      <td><%= taxi.pickupAddy %></td>
                      <td><%= taxi.pickupCity %></td>
                      <td><%= taxi.departs %></td>
                      <td><a href="/taxi/<%= taxi._id %>/update">Edit Trip</a></td>
                  </tr> 
              </tbody>
          </table>
        </div><br>
        <div class="passenger-info">
            <% if (passengers.length) { %>
              <h2>Passengers:</h2>
              <table class="table">
                <thead>
                  <tr>
                    <th>Seats</th>
                    <th>Price</th>
                  </tr>
                </thead>
                <tbody>
                  <% passengers.forEach(function(p) { %>
                    <tr>
                      <td><%= p.noOfSeats %></td>
                      <td>$<%= p.cost %></td>
                    <td>
                    <form action="/passengers/<%= p._id %>?_method=DELETE" method="POST">
                      <input id="delete-passenger" type="submit" name="" value="Delete Passenger"> 
                    </form>
                    </td>
                  </tr>
                  <% }); %>
                </tbody>
              </table> <br>
            <% } else { %>
              <h3>No Passenger Information Availiable <button><a href="/taxi/<%= taxi._id %>/passengers/new">Add Passengers</a></button></h5>
            <% } %>
        </div>
            
      
        <button><a href="/taxi">Home</a></button><br>
        <button><a href="/taxi/new">Add Ride</a></button>

<%- include('../partials/footer') %>